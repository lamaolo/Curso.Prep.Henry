1 - Arrays:

Un array a diferencia de una variable o constante, nos permite almacenar tantos datos como queramos, los cuales podemos acceder usando el indice de cada elemento. De esta forma, si tenemos 5 personas diferentes, en vez de declarar 5 variables para cada una, podriamos agruparlas todas en un solo array.